[project]
name = "traceproof-model-server"
version = "0.1.0"
description = "Private GPU inference engine for TraceProofAI"
requires-python = ">=3.10,<3.14"

[tool.ruff]
line-length = 110
target-version = "py310"
